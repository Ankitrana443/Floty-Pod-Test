# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Floty Pod Test' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
pod 'Floaty', '~> 4.1.0'
  # Pods for Floty Pod Test

  target 'Floty Pod TestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Floty Pod TestUITests' do
    # Pods for testing
  end

end
